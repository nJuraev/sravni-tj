# Снижение расхода токенов парсера — выжимка

Контекст: расход на DeepSeek API вырос (~$1.75-3.5/день), разбирались почему и
чинили. Ссылки на код/строки могли сместиться после дальнейших правок —
сверяйся с реальным файлом, не только с этим.

## Статус git — всё из этого документа уже закоммичено

- `5c21412`, `a2b9eb5` — дедуп повторных строк, `_en`-стрип у Арванда,
  DeepSeek-схема-текст, header/footer/nav-вырез, IBT `faq-section`/`cform`-вырез.
- `1e5a96f` ("optimize parser") — skip-if-unchanged кэш целиком (часть 2 ниже).
- `01cd02e` ("correct discovery and parser", 27.08) — удаление auto-fallback
  в `cmd/parser` (часть 3 ниже) + миграции `add_notes_to_bank_source_urls`,
  `add_static_source_kind`, `add_sequential_ids_kind`, новые
  `extract/links.go`, `extract/staticsource.go`, переезд `retry.go` в свой
  пакет. **Эта часть коммита сделана НЕ в рамках задокументированной здесь
  работы** — детали/причины этих файлов в этом документе не описаны, сверяйся
  с кодом напрямую, если понадобится.
- `aca8fc0` ("fix amonat parser", 03.09) — display:none/hidden-вырез (часть 5
  ниже). В этом же коммите — не связанная фича (блог/статьи), не имеет
  отношения к парсеру.
- Миграция `2026_08_16_000001_add_content_hash_to_products_and_sources` —
  прогнана локально; **прогнана ли на проде — не проверено из этого чата,
  сверить перед следующим шагом.**

---

## Часть 1 — почему рос расход (диагностика)

1. **DeepSeek не получал JSON Schema вообще.** Комментарий в коде врал, что "схема
   задаётся текстом промпта" — на деле нет. Фикс: `extract.PromptVersion` +
   `schemaPromptText()`/`stripSchemaDescriptions()` в `deepseek.go` — теперь схема
   (без задвоенных `description`, они и так есть в systemPrompt) реально уходит в
   промпт DeepSeek. Вероятная причина бага "ставка=0" у Арванда (`interest_rate:
   "31% - 32%"` без схемы rate_tiers модель не понимала, куда класть текстовый диапазон).
2. **Markdown, уходящий в AI, на 20-90% шум**: повторяющееся меню (шапка+моб.меню+
   sitemap в футере — дублируются буквально), `<header>/<footer>/<nav>` (даже без
   дублирования — просто шум по HTML5-семантике), FAQ/форма заявки (IBT: `faq-section`/
   `cform` по id), `_en`-поля у Арванда (не нужны, только ru/tg).
3. **Все фиксы шума — универсальные regex в `parser/internal/scrape/direct.go`**
   (`reCut`, `reNoiseBlocks`, `reHeader/reFooter/reNav`, `dedupLines`) — no-op там,
   где не применимо, без риска (в отличие от css_hints/CSS-селекторов — сознательно
   не стали делать, см. `docs/specs/parser.md:86` про хрупкость к вёрстке).

### Замеры до/после (реальные прогоны, `htmlToText` на живых страницах)

| Банк | Было | После дедупа | +header/footer/nav |
|---|---|---|---|
| eskhata (car) | 14520 | 10823 (-25%) | — |
| humo (дискавери) | — | 3142 | 2187 (-30%) |
| imon (продукт) | — | 4473 | 2856 (-36%) |
| spitamenbank (каталог) | — | 4564 | 3180 (-30%) |
| alif (продукт) | — | 6593 | 4934 (-25%) |
| IBT (5-продуктовая, `kredit-na-neobkhodimye-nuzhdy`) | — | 11676 | 9278 (-20.5%, `faq-section`/`cform`) |

**Spitamenbank — важное исключение:** FAQ-блок там (`sb-question`/`sb-item`)
содержит первым пунктом «Необходимые документы» — реальные данные для
`documents_ru`. Резать весь блок нельзя (в отличие от IBT, где FAQ — чистый шум).
**Не тронуто сознательно.**

---

## Часть 2 — Skip-if-unchanged кэш

Идея: если текст, уходящий в AI, побайтово не изменился с прошлого успешного
прогона — не звать AI вообще, только подтвердить `parsed_at`. **Закоммичено,
`1e5a96f`.**

### Архитектурное решение: Postgres, не файлы

Пользователь предлагал `.txt`-файлы на диске. Проверил `docs/railway-deploy.md` +
`parser/railway.json` — у parser-сервиса на Railway **нет persistent volume**,
`restartPolicyType: NEVER`, каждый крон-тик — новый контейнер. Файлы не
переживут прод. Кэш — в Postgres, 2 новые nullable-колонки:

- `bank_source_urls.last_markdown_hash` — хэш верхнеуровневой страницы задачи.
- `products.content_hash` — хэш текста конкретного продукта (для array-split —
  текста конкретного элемента массива).
- Новый индекс `idx_products_source_url_lookup` на `(source_url_id, source_url)`.

### Версия промпта в хэше (критично)

`contentHash()` в `parser/internal/parser/cache.go` хэширует
`extract.PromptVersion + "|" + текст`, не просто текст. **Без этого** — если
поменяли systemPrompt/схему (за сессию делали несколько раз), уже спарсенные
продукты навсегда остались бы со старой логикой, пока сайт банка сам не
поменяется (иначе хэш совпадёт и кэш скипнет AI, хотя логика экстракции стала другой).
**Бампать `PromptVersion` (`extract/extract.go`) на любое смысловое изменение
systemPrompt/responseSchema/schemaPromptText.**

### Механика

- **Прямой путь** (большинство банков): `parser.go` перед AI-вызовом сравнивает
  `sha256(PromptVersion+markdown)` с `bank_source_urls.last_markdown_hash`. Совпало
  → `trySkipUnchanged()` (`cache.go`) проверяет `DistinctProductSourceURLs` — если
  это ровно один URL = `task.URL` — просто `TouchProductsBySourceURL` (bump
  `parsed_at`), AI не вызывается.
- **Array-split** (Арванд/ICB/SSB): у каждого элемента массива свой `source_url =
  task.URL + "#" + naturalID` (slug/id элемента, см. `naturalElementID()` в
  `arraysplit.go`) — иначе все элементы одной задачи делили бы один `source_url`
  и были бы неразличимы для кэша. Хэш проверяется `ProductContentHash()` по
  каждому элементу отдельно ДО AI-вызова.
- **Хэш пишем только при `!anyRejected`** (ни одна карточка не забракована
  валидацией в этом прогоне) — иначе забракованная карточка навсегда выпала бы
  из кэша (нет строки в products → вечно считается новой). Честно пустой прогон
  (AI просто ничего не нашёл, не брак) — хэш пишем, это ок.
- **Index-режим (изначально планировался как "фаза 2b") отменился сам** — после
  части 3 (удаление auto-fallback) `cmd/parser` больше не умеет в index-режим
  вообще, значит и кэшировать там нечего.

### Живая проверка (реальные прогоны, не только юнит-тесты)

- Эсхата `/loans/car/` (прямой путь): прогон 1 — 37 сек, реальный AI-ответ.
  Прогон 2 (без изменений на сайте) — 919мс, `ai_raw_response` пуст, `parsed_at`
  обновился.
- Арванд credits (array-split, 7 элементов): прогон 1 — 6 успешно + 1 упал EOF.
  Прогон 2 — 6 из 7 пропущены (0 AI-вызовов), 7-й (ранее падавший) реально
  спарсился и получил хэш.

### Критика/ограничения (обсуждали, стоит помнить)

- Крон стоит `0 3 */10 * *` (раз в 10 дней) — специально уменьшен, потому что
  парсер жрал токены впустую на кривых прогонах. После фиксов частоту
  можно/нужно поднять обратно — тогда кэш станет полезнее (больше прогонов =
  больше шансов скипнуть).
- Проверил HTTP conditional GET (`ETag`/`Last-Modified`) как альтернативу
  собственному хэшу — не подходит: все проверенные банки шлют
  `Cache-Control: no-store, no-cache, must-revalidate`, ни `ETag`, ни
  `Last-Modified` нет вообще.
- Ссылки в тексте (PDF-пути с CMS-хэшами в URL и т.п.) — потенциальный источник
  ложных "изменений" (хэш меняется, хотя по сути ничего не поменялось). Не
  критично — просто лишний AI-вызов, не порча данных.

---

## Часть 3 — Архитектура discover/parse

Пользователь: два независимых пайплайна — **discover** ищет ТОЛЬКО ссылки на
страницы продуктов (по явным `bank_parse_instructions.notes`/`menu_sections`),
**parse** читает ВСЕ `bank_source_urls` и извлекает текст/условия, без
собственного поиска ссылок. **Закоммичено, `01cd02e`.**

**Было не так:** `cmd/parser` имел свой встроенный "auto-fallback" — если AI при
парсинге детекчил `product_links` (страница вдруг каталог) — сам рекурсивно
обходил ссылки (`gatherFromLinks`/`resolveDetailLinks`, до 40 страниц). Дублировало
работу discover.

**Убрано полностью:** `gatherFromLinks`, `resolveDetailLinks`, `detailJob`,
`maxDetailPages`, `stripLinkSyntax`, `sameSite`, `regDomain`, `normalizeURL` —
все удалены из `parser.go`. Теперь если AI всё же вернёт `product_links` —
просто `p.log.Warn(...)`, без обхода. Сигнал "иди чини discovery-инструкцию",
не тихая компенсация.

`cmd/discover` не трогали в этой части — там логика (если ссылок нет, но
продукты нашлись прямо на стартовой странице — регистрировать саму страницу) —
это его легитимная работа, не дублирование.

**Примечание:** этот же коммит (`01cd02e`) добавил заметно больше, чем описано
здесь — `extract/links.go`, `extract/staticsource.go`, миграции
`add_static_source_kind`/`add_sequential_ids_kind`, переработку
`discover.go` (285 строк изменений). Это, судя по всему, дальнейшее развитие
discovery вне рамок этого документа — не разбиралось в чате, который его
написал, сверяться напрямую с кодом.

### Аудит `bank_parse_instructions` (kind='product_discovery') — 4 находки

1. **amonatbank/deposit (id=14)** — note: *"если каталог 500 — продукты в меню
   главной"*. Код не может это выполнить: `Scrape()` ошибка → `discover.process`
   делает `return` до вызова AI. Инструкция для AI, который её не увидит.
   **Статус на момент написания — не починено**, но после `01cd02e` могло
   измениться (см. примечание выше) — проверить актуальное состояние в коде.
2. **oriyonbank credit+deposit (id=16,17)** — сайт за Cloudflare JS-challenge
   (403 "Attention Required" через Direct стабильно). Починено (dev):
   `scraper='browser'` на инструкциях И на `bank_source_urls`. Проверить,
   применено ли на проде.
3. **dbt credit+deposit (id=26,27)** — React SPA, прямой GET = пустой
   `<div id="app">`. Починено (dev): `scraper='browser'` аналогично. Проверить прод.
4. **alif** — discovery-инструкция была только под `deposit`. `installment`-источник
   (`/ru/auto/cars`) в `bank_source_urls` — сирота без своей инструкции, вне
   пайплайна discover→parse. Статус — сверить с кодом после `01cd02e`.

---

## Часть 4 — Замер эффекта кэша на реальных прод-логах

Сравнил логи прод-крона 31.08 и 01.09 (после деплоя части 2 и 3).

| | 31.08 | 01.09 |
|---|---|---|
| Всего "было бы AI-вызовов" | 195 | 197 |
| Пропущено (хэш совпал) | 69 | 83 |
| **Skip-rate** | 35.4% | **42.1%** |
| Реальных AI-вызовов | 126 | 114 (-9.5%) |

Кэш работает и раскачивается день ко дню, как задумано. Но расход $ упал
меньше, чем росли пропуски (2.00→1.93, -3.5% против -9.5% по вызовам) — нашёл
причину в части 5.

---

## Часть 5 — amonatbank: настоящая причина постоянных "decode extraction: EOF"

Из лог-анализа (часть 4): ~15-20 источников падают с ошибкой
`ai_error: deepseek: decode extraction: EOF` КАЖДЫЙ прогон, оба дня подряд,
без единого успеха — 52 из 58 ошибок за 2 дня. Amonatbank — 11 URL
(все loans/deposits) — самый частый и стабильный повторитель. Раз хэш при
отбраковке/ошибке НЕ пишется (осознанное решение части 2, чтобы не терять
шанс на переизвлечение) — эти источники **не могут** получить пользу от кэша
в принципе и жгут токены впустую на каждом прогоне.

### Корень найден: сайт банка сам прячет свою PHP-ошибку через CSS

Дампнул реальный markdown, отправляемый в AI для
`amonatbonk.tj/ru/personal/loans/potrebitelskiy-kredit/` (через headless
Chrome, как в проде) — внутри оказался ПОЛНЫЙ PHP stack trace (Bitrix,
`Undefined constant "DSC"` / `TypeError`), рендерящийся прямо в теле страницы.
Источник — сломанный компонент шаблона `news.detail`, общий для всего раздела
`loans/`+`deposits/` (отсюда одинаковая ошибка на ~11 страницах).

Сайт сам прячет этот вывод от живых пользователей через CSS — но **двумя
разными способами на разных страницах**:
- `<h2 style="display:none;"><pre>[Error]...` — инлайн-style.
- `<div class="d-none questionsCont ..."><pre>[TypeError]...` — Bootstrap-класс
  `d-none`, не инлайн-style.

Текстовый скрейпер CSS не понимает и честно тащит невидимый стектрейс в AI —
подтверждённая причина стабильного EOF на этих страницах.

### Фикс (`aca8fc0`, `parser/internal/scrape/direct.go`)

`reHiddenUnconditional`/`reHiddenByClass` — режут содержимое div/span/p/pre/h1-h6,
скрытое любым из: `style="display:none"`, класс `d-none`/`hidden`/`invisible`,
нативный HTML5-атрибут `hidden`.

**Важный нюанс, найденный при ручной проверке** (пользователь попросил достать
3-4 реальных блока для проверки — не поверил на слово, и правильно сделал):
класс `d-none` у Bootstrap используется и для **responsive show/hide**
(`d-none d-md-block` = скрыт на мобиле, показан от md) — это НЕ "невидимо
никому", а "видимо на части экранов". Первая версия фикса резала такие блоки
безусловно и случайно задела бы реально видимую десктопную картинку на той же
странице (`<div class="position-absolute Img d-none d-md-block ..."><img ...
alt="Потребительский кредит"></div>`).

**Исправлено:** `reHiddenByClass` пропускает совпадение (не режет), если в
том же теге есть `d-{sm,md,lg,xl,xxl}-{block,flex,inline,inline-block,grid,
table,table-cell}` (`reResponsiveDisplay`) — считается видимым на каком-то
брейкпоинте. `style=display:none` и HTML5 `hidden` такого нюанса не имеют
(однозначны при любом viewport) — их резка не изменилась.

**Результат на живых страницах:**
- `/loans/potrebitelskiy-kredit/` — 2847 → 488 рун (-83%), стектрейс ушёл,
  ставка/сумма/срок остались, никакой видимый контент не потерян.
- `/deposits/srochnyy-vklad/` — 3257 → 896 рун (-72%), аналогично.

Юнит-тесты (`direct_test.go`): `TestHtmlToText_HiddenDisplayNoneStripped` (все
3 способа скрытия режутся), `TestHtmlToText_ResponsiveDNoneNotStripped`
(responsive-класс НЕ режется, безусловно скрытый `d-none` без responsive —
режется).

**Не проверено:** останутся ли ещё какие-то причины EOF на amonatbank после
этого фикса (стектрейс — подтверждённая, но не обязательно единственная
причина) — нужно посмотреть логи следующего прод-прогона после деплоя `aca8fc0`.

---

## Что дальше (TODO)

1. Свериться с прод-логами ПОСЛЕ деплоя `aca8fc0` — упал ли % `ai_error` у
   amonatbank и других банков с похожим Bitrix/CMS-паттерном (d-none/hidden
   debug-вывод — стоит поискать и на других банках, не только amonatbank).
2. Проверить/прогнать на проде миграцию `2026_08_16_000001_...`, если ещё не
   прогнана.
3. Свериться с кодом (`bank_parse_instructions`, `bank_source_urls`) — актуальны
   ли ещё находки 1/2/3/4 из части 3 после `01cd02e`, что из починенного в dev
   доехало до прода.
4. Понаблюдать hit-rate кэша (часть 2/4) ещё за пару прод-циклов — решить,
   стоит ли поднимать частоту крона с раз-в-10-дней.
5. Курсовой пайплайн (`cmd/rates`, свой крон раз в 2 часа) — не разбирался
   вообще, весь документ про credit/deposit-парсинг продуктов.
