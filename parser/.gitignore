# Скомпилированные бинарники (якорь на корень parser/, не рекурсивно —
# иначе матчит cmd/parser и internal/parser как директории с тем же именем)
/app
/parser
*.exe
*.out

# Секреты
.env
.env.*
!.env.example

# Логи
*.log
