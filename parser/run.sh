#!/bin/sh
# Railway cron entrypoint: discovery -> парсинг продуктов, последовательно за
# один прогон. Курсы валют — отдельный сервис (run-rates.sh), своё расписание.
# Без && — падение одного шага не должно блокировать следующий.
set -x

./discover
./parser
