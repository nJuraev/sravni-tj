#!/bin/sh
# Railway cron entrypoint: discovery -> парсинг продуктов -> курсы валют,
# последовательно за один прогон. Без && — падение одного шага не должно
# блокировать следующие (каждый бинарник сам логирует свои ошибки).
set -x

./discover
./parser
./rates
